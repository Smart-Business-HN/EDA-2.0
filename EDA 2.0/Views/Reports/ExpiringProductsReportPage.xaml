<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="EDA_2._0.Views.Reports.ExpiringProductsReportPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="Transparent"
    Loaded="Page_Loaded">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="Productos por Vencer" FontSize="28" FontWeight="SemiBold"/>
            <TextBlock Text="Productos proximos a su fecha de vencimiento"
                       FontSize="14"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Filters -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="12" Margin="0,0,0,16">
            <ComboBox x:Name="DaysComboBox"
                      Header="Dias de anticipacion"
                      MinWidth="150"
                      SelectedIndex="0">
                <ComboBoxItem Content="30 dias" Tag="30"/>
                <ComboBoxItem Content="60 dias" Tag="60"/>
                <ComboBoxItem Content="90 dias" Tag="90"/>
            </ComboBox>
            <Button Content="Buscar"
                    Style="{StaticResource AccentButtonStyle}"
                    Click="SearchButton_Click"
                    VerticalAlignment="Bottom"/>
            <ProgressRing x:Name="LoadingRing" IsActive="False" Width="24" Height="24" VerticalAlignment="Bottom"/>
        </StackPanel>

        <!-- Summary Cards -->
        <Grid Grid.Row="2" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    CornerRadius="8" Padding="16" Margin="0,0,8,0">
                <StackPanel>
                    <TextBlock Text="Vencidos" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <TextBlock x:Name="TotalExpiredText" Text="0" FontSize="28" FontWeight="Bold" Foreground="Red"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    CornerRadius="8" Padding="16" Margin="4,0,4,0">
                <StackPanel>
                    <TextBlock Text="Por Vencer" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <TextBlock x:Name="TotalExpiringText" Text="0" FontSize="28" FontWeight="Bold" Foreground="Orange"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    CornerRadius="8" Padding="16" Margin="8,0,0,0">
                <StackPanel>
                    <TextBlock Text="Valor en Riesgo" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <TextBlock x:Name="TotalValueText" Text="L 0.00" FontSize="28" FontWeight="Bold"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Data Grid -->
        <ListView Grid.Row="3" x:Name="DataListView" SelectionMode="None">
            <ListView.Header>
                <Grid Padding="12,8" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="4" Margin="0,0,0,4">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="130"/>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="80"/>
                        <ColumnDefinition Width="80"/>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="90"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Producto" FontWeight="SemiBold"/>
                    <TextBlock Grid.Column="1" Text="Familia" FontWeight="SemiBold"/>
                    <TextBlock Grid.Column="2" Text="Vencimiento" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                    <TextBlock Grid.Column="3" Text="Dias" FontWeight="SemiBold" HorizontalAlignment="Right"/>
                    <TextBlock Grid.Column="4" Text="Stock" FontWeight="SemiBold" HorizontalAlignment="Right"/>
                    <TextBlock Grid.Column="5" Text="Valor Total" FontWeight="SemiBold" HorizontalAlignment="Right"/>
                    <TextBlock Grid.Column="6" Text="Estado" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                </Grid>
            </ListView.Header>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="12,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="130"/>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="80"/>
                            <ColumnDefinition Width="80"/>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="90"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{Binding ProductName}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                        <TextBlock Grid.Column="1" Text="{Binding FamilyName}" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="2" Text="{Binding ExpirationDateFormatted}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        <TextBlock Grid.Column="3" Text="{Binding DaysUntilExpiration}" VerticalAlignment="Center" HorizontalAlignment="Right" FontWeight="SemiBold"/>
                        <TextBlock Grid.Column="4" Text="{Binding CurrentStock}" VerticalAlignment="Center" HorizontalAlignment="Right"/>
                        <TextBlock Grid.Column="5" Text="{Binding TotalValueFormatted}" VerticalAlignment="Center" HorizontalAlignment="Right"/>
                        <Border Grid.Column="6" Background="{Binding StatusBackground}" CornerRadius="4" Padding="8,4" HorizontalAlignment="Center">
                            <TextBlock Text="{Binding Status}" FontSize="11" FontWeight="SemiBold"/>
                        </Border>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0" Spacing="8">
            <Button x:Name="PrintPdfButton"
                    Content="Imprimir PDF"
                    Click="PrintPdf_Click"
                    IsEnabled="False"/>
        </StackPanel>
    </Grid>
</Page>
